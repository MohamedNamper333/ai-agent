# ─────────────────────────────────────────────
#  AI Agent — Complete Requirements
#  Generated: 2026-06-10
#  Run: pip install -r requirements.txt
# ─────────────────────────────────────────────

# ── Web Framework ─────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
python-multipart>=0.0.9
sse-starlette>=1.8.0
starlette>=0.36.0

# ── Production Server ─────────────────────────
gunicorn>=21.2.0

# ── HTTP Clients ──────────────────────────────
requests>=2.31.0
httpx>=0.27.0                  # Required by FastAPI TestClient

# ── Authentication & Security ─────────────────
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0
cryptography>=42.0.0

# ── Database ──────────────────────────────────
# SQLite is built-in; add these for future PostgreSQL migration
sqlalchemy>=2.0.0
alembic>=1.13.0
# psycopg2-binary>=2.9.0       # Uncomment for PostgreSQL

# ── Caching ───────────────────────────────────
# redis>=5.0.0                 # Uncomment for Redis cache (Phase 4)

# ── Data Analysis ─────────────────────────────
pandas>=2.1.0                  # MISSING in original — used in data_analysis.py
numpy>=1.26.0                  # Used in vector_store.py
scipy>=1.12.0                  # Optional: stats, clustering

# ── Machine Learning / Embeddings ─────────────
sentence-transformers>=2.6.0   # Primary embedder
# chromadb>=0.4.0              # Uncomment for Phase 1 RAG upgrade
# qdrant-client>=1.8.0         # Alternative vector store

# ── LLM Providers ─────────────────────────────
openai>=1.14.0                 # OpenCode Zen + GPT-4 provider
# anthropic>=0.25.0            # Uncomment to add Claude API provider
# gpt4all>=2.7.0               # Uncomment for GPT4All backend

# ── Document Processing ───────────────────────
PyPDF2>=3.0.0
python-docx>=1.1.0
openpyxl>=3.1.0                # MISSING — used in data_analysis.py, documents.py
xlrd>=2.0.1                    # Legacy Excel format support

# ── Image Processing ──────────────────────────
Pillow>=10.0.0
# pytesseract>=0.3.10          # Uncomment for OCR (needs Tesseract installed)
# pydub>=0.25.1                # Uncomment for audio processing

# ── Voice & TTS ───────────────────────────────
gTTS>=2.5.0
# SpeechRecognition>=3.10.0    # Uncomment for voice input
# pyttsx3>=2.90                # Uncomment for offline TTS
# openai-whisper>=20230918     # Uncomment for Whisper transcription

# ── Web Scraping ──────────────────────────────
beautifulsoup4>=4.12.0         # Used in web_search.py
lxml>=5.1.0                    # Parser backend for bs4

# ── Monitoring & Observability ────────────────
# opentelemetry-sdk>=1.24.0    # Uncomment for Phase 3
# prometheus-client>=0.20.0    # Uncomment for Phase 3

# ── Testing ───────────────────────────────────
pytest>=8.1.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
httpx>=0.27.0                  # FastAPI test client
anyio>=4.3.0

# ── Code Quality ──────────────────────────────
ruff>=0.3.0                    # Fast linter + formatter
mypy>=1.9.0                    # Type checking

# ── Utilities ─────────────────────────────────
python-dotenv>=1.0.0           # .env file loading
rich>=13.7.0                   # Better CLI output
click>=8.1.0                   # CLI framework
pyyaml>=6.0.1                  # YAML config support
toml>=0.10.2                   # TOML config support
